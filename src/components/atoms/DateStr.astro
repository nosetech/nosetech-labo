---
const { class: className, date } = Astro.props;
---

<time class={className} datetime={date}>
  {
    new Date(date).toLocaleDateString("ja-JP", {
      year: "numeric",
      month: "2-digit",
      day: "2-digit",
    })
  }
</time>
<style>
  time {
    color: var(--color-text);
  }
</style>
